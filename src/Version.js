/**
 * @class Version
 */
class Version {
    /**
     * @constructor
     * @param {*} pkg package
     * @param {*} opt version payload
     */
    constructor(pkg, opt) {
        this.package = pkg;
    }
}

module.exports = Version;
